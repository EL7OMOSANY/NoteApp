import 'package:flutter/material.dart';
import 'package:note_book_app/widgets/node_detail.dart';

class CustomNote extends StatelessWidget {
  const CustomNote({super.key});

  @override
  Widget build(BuildContext context) {
    return NoteDetail();
  }
}
